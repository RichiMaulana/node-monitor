const processMonitorRepo = require('./processMonitorRepo')
const diskMonitorRepo = require('./diskMonitorRepo')
const networkMonitorRepo = require('./networkMonitorRepo')

module.exports = {
    processMonitorRepo,
    diskMonitorRepo,
    networkMonitorRepo
}